//const utils = require('./utils'); -> import everything
const {greeting, farwell} = require('./utils')

//console.log(utils)
//utils.greeting('Alice'); -> when importing everything.
//utils.farewell('Alice'); -> when importing everything.

greeting('Alice'); // -> when importing only some functions.
greeting('Alice'); // -> when importing only some functions.